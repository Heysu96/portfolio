// Re-export types from supabase/types for backward compatibility
export type { Category, MediaItem, Project } from "@/lib/supabase/types";

// Note: Project data is now managed in Supabase database
// Use the queries from @/lib/queries to fetch data
